<div class="card">
    <a href="/post/{{id}}">
        <h2>{{title}}</h2>
        <div class="postContent">
            <p>{{content}}</p>
            <p>Posted by {{username}} on {{date}}</p>
        </div>
    </a>
</div>